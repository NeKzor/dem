{
  "tasks": {
    "gen": "deno run --allow-read=.dem.json,.dem.map.json,docs --allow-write=docs gen/main.ts"
  },
  "lint": {
    "rules": {
      "exclude": [
        "no-fallthrough"
      ]
    }
  }
}
